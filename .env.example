# Optional future provider configuration. MVP uses Codex CLI / JSON import, not production API keys.
DATABASE_PATH=data/lead_discovery.sqlite

# Future email digest config; leave unset unless explicitly enabling send.
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_SENDER=
DIGEST_RECIPIENT=

# Future LLM provider adapters; not required for MVP.
LLM_PROVIDER=
LLM_API_KEY=
